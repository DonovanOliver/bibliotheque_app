<%= form_for(@laboratoire) do |f| %>
  <% if @laboratoire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@laboratoire.errors.count, "error") %> prohibited this laboratoire from being saved:</h2>

      <ul>
      <% @laboratoire.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
